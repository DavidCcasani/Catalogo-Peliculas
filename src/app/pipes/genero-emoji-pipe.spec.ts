import { GeneroEmojiPipe } from './genero-emoji-pipe';

describe('GeneroEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneroEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
